# AUTOGENERATED ${timestamp()}
%{ for idx, instance in instances ~}
%{ for tag_n, tag_v in instance.tags ~}
terraform import aws_ec2_tag.ec2_${instance.id}_${tag_n} ${instance.id},"${tag_n}"
%{ endfor ~}
%{ endfor ~}